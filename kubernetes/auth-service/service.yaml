apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - name: http-8080  # <-- THÊM ĐÚNG DÒNG NÀY ĐỂ PROMETHEUS NHÌN THẤY
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP